name: Günlük M3U Güncelleme

on:
  workflow_dispatch:
  schedule:
    - cron: '30 3 * * *'  # Her gün saat 03:30 UTC (Türkiye saatiyle 06:30)

jobs:
  update-m3u:
    runs-on: ubuntu-latest

    steps:
      - name: Depoyu klonla
        uses: actions/checkout@v3

      - name: Node.js kur
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Gerekli paketleri yükle
        run: npm install axios

      - name: Scripti çalıştır
        run: node update.js

      - name: Değişiklikleri commit et ve pushla
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add mehmet_guncel_modified.m3u
          git commit -m "🔄 Otomatik güncelleme: $(date +'%Y-%m-%d %H:%M')" || echo "Değişiklik yok, commit atlanıyor"
          git push
